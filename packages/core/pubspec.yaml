name: fieldup_core
description: FieldUp shared core — models, utils, extensions, error types

publish_to: none

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  freezed_annotation: ^2.4.4
  json_annotation: ^4.9.0
  equatable: ^2.0.5
  intl: ^0.19.0

dev_dependencies:
  build_runner: ^2.4.11
  freezed: ^2.5.2
  json_serializable: ^6.8.0
  flutter_test:
    sdk: flutter
